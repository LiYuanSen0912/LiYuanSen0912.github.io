<!doctype html>
<html lang="<%= config.language %>">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= config.description %>">
    <title><%= page.title ? page.title + ' · ' : '' %><%= config.title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Noto+Serif+SC:wght@400;500;600;700&family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<%- url_for('/css/style.css') %>">
  </head>
  <body>
    <div class="grain" aria-hidden="true"></div>
    <header class="site-header">
      <a class="brand" href="<%- url_for('/') %>" aria-label="回到首页">LYS<span>·</span></a>
      <nav aria-label="主导航">
        <a href="<%- url_for('/') %>">文章</a>
        <a href="<%- url_for('/about/') %>">关于</a>
      </nav>
      <button class="theme-button" type="button" aria-label="切换颜色主题">◐</button>
    </header>
    <main>
